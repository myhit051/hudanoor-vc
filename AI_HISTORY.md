# ประวัติคำขอของบอส (HUDANOOR) — อ่านก่อนแก้ฟีเจอร์ขาย/จัดส่ง

เรียงตามลำดับเวลา (ทั้งหมดทำเมื่อ 17 ก.ย. 2026) · แต่ละข้อ: บอสขออะไร → ทำอะไร → เหตุผล/ข้อสังเกต

1. **ค่าส่งเป็นรายรับ** — "ก่อนบรรทัดส่วนลด ให้บันทึกค่าส่งได้ (ไม่บังคับ) นับรวมยอดขาย"
   - รอบแรกทำเป็นช่องต่อสินค้า → รีวิวแล้วพบว่านับซ้ำได้ เลยย้ายเป็น **ต่อออเดอร์** (คอลัมน์ `shipping_fee`, บวกเข้า `total_amount` ของแถวแรก)
   - รอบย้ายไปอยู่ในกล่องตะกร้า ซึ่งซ่อนอยู่จนกว่าจะเพิ่มสินค้า → บอสถาม "ทำไมไม่มีช่องค่าส่งแล้ว" → ย้ายมาอยู่ส่วนหัวออเดอร์ แสดงตลอด
   - เหตุผลของบอส: ลูกค้าโอนรวมค่าส่ง ยอดรวมในระบบต้องตรงกับสลิป

2. **เมนูจัดส่ง** — เลือกออเดอร์ไปพิมพ์ที่อยู่เป็น PDF, ค่าเริ่มต้นแสดงเฉพาะออนไลน์, มีแท็ก รอส่ง/ส่งแล้ว/ตีกลับ
   - ทำหน้า `/shipping` + คอลัมน์ `shipping_status` + PATCH แบบหลายออเดอร์
   - ใบปะหน้าทำด้วย html2canvas + jsPDF (ใช้ตามแบบ `PayslipDialog.tsx`) เพื่อให้ภาษาไทยถูกต้องโดยไม่ต้องฝังฟอนต์ใน jsPDF
   - ต่อมาบอสขอ **A4 8 ใบ/หน้า** → เพิ่มแบบย่อ (`CompactShippingLabel`)

3. **รีวิว (/scrutinize)** เจอ 5 ข้อ บอสสั่งแก้ทั้งหมด:
   - เลขออเดอร์ซ้ำหลังลบ (COUNT+1) → ตาราง `order_counters`
   - `GET /api/sales` เปิดสาธารณะ (ข้อมูลลูกค้าหลุด) → ต้องล็อกอิน
   - ค่าส่งต่อสินค้านับซ้ำ → ต่อออเดอร์
   - กำไรรวมค่าส่ง → หักออก (มาร์จิ้นคิดจากค่าสินค้า)
   - หน้าจัดส่ง: สวิตช์เฉพาะมีที่อยู่ / ไม่ล้างรายการที่เลือก / ตัดที่อยู่ร้านยาวในแบบย่อ
   - จากนั้นบอสสั่ง "ปิด" การบันทึกขายแบบไม่ล็อกอิน → `POST /api/sales` ต้องล็อกอินด้วย

4. **Admin บันทึกแทนคนอื่น** — ช่องผู้บันทึกเป็น dropdown สำหรับ admin (รายชื่อจาก `/api/auth?action=user-names`)
   - เซิร์ฟเวอร์: ไม่ใช่ admin แต่ส่งชื่อคนอื่น → 403 · ชื่อที่ไม่ใช่บัญชี active → 400
   - เหตุผล: ยอด/ค่าคอมต้องตกเป็นของพนักงานที่ขายจริง

5. **บันทึกแล้วกรอกคนถัดไป** — คงช่องทาง/แอดมิน(ผู้บันทึก)/สาขา ล้างแค่ตัวเลขและสินค้า
   - ปุ่มเดิมทำได้เกือบหมดแล้วแต่ชื่อไม่สื่อ → เปลี่ยนชื่อเป็น "บันทึก → ออเดอร์ถัดไป" + ล้างฟอร์มสินค้าที่ค้าง + คำอธิบายใต้ปุ่ม
   - ที่อยู่จัดส่งถูกล้างด้วย (เป็นของลูกค้าแต่ละคน กันติดไปใบปะหน้าคนถัดไป)

6. **COD** — เลือก COD ตอนบันทึก และขึ้นในใบปะหน้าให้เห็นชัด
   - คอลัมน์ `payment_method` (เฉพาะออนไลน์), สวิตช์ในกล่องที่อยู่, แถบดำ COD + ยอดเก็บบนใบปะหน้าทุกขนาด, ป้าย COD ในรายการ
   - ยังไม่มี: ปุ่มเปลี่ยน COD ย้อนหลังให้ออเดอร์เก่า (เสนอบอสไว้แล้ว ยังไม่ได้สั่ง)

## งานที่ค้าง / ไอเดียที่เสนอไว้
- ปุ่มเปลี่ยนวิธีชำระ (COD/โอน) ย้อนหลังในหน้าจัดส่ง
- ยังไม่เคยทดสอบบันทึก/เปลี่ยนสถานะบนเว็บจริงด้วยบัญชีที่ล็อกอิน (ตั้งใจไม่สร้างข้อมูลทดสอบ)
